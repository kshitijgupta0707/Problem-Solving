#include <bits/stdc++.h>
using namespace std;
// bits
// test
// inarr
// outarr
class Solution
{
public:
    // bool checkSubarraySum(vector<int>& nums, int k) {
    //     //brutforce
    //      int n = nums.size() ;
    //      for(int i = 0 ; i < n ; i++){
    //         int sum = nums[i];
    //         for(int j = i + 1 ; j < n ; j++){
    //             sum += nums[j];
    //             if(sum % k == 0){
    //                 if(j - i + 1 >=2){
    //                     return true;
    //                 }
    //             }
    //         }
    //      }
    //      return false;
    // }
    // i am doing hashing where we will be storing the mod value in the unordred map
    bool checkSubarraySum(vector<int> &nums, int k)
    {
        
    }
};